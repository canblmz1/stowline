FROM python:3.12-slim
WORKDIR /app
COPY server /app
COPY release /release
RUN pip install --no-cache-dir -e ".[psycopg]"
# release/manifest.json: version and the agent pin for automatic upgrades
ENV STOWLINE_RELEASE_DIR=/release
EXPOSE 8080
CMD ["stowline-server"]
